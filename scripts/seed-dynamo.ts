/**
 * Seed scriviq-main DynamoDB table.
 * Run: npx tsx scripts/seed-dynamo.ts
 *
 * The application no longer ships with pre-seeded demo data.
 * Users upload their own contracts — the workspace starts empty.
 */

console.log("Nothing to seed — workspaces start empty. Upload contracts via the UI.");
